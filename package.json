{
  "name": "hero-browser",
  "repository": "https://github.com/sonnyg/electron-hero-browser.git",
  "license": "MIT",
  "version": "0.1.0",
  "main": "main.js",
  "dependencies": {
    "dotenv": "^2.0.0",
    "md5": "^2.1.0"
  },
  "scripts": {
    "package": "electron-packager . HeroBrowser --platform=darwin --arch=x64 --version=1.3.5 --out=./build --overwrite --icon=./assets/icons/marvel-app.icns"
  }
}
